---
- name: Run httpd proxy
  docker_container:
    name: httpd
    image: amelkdr/tp-devops-httpd:latest
    ports:
      - "80:80"
    networks:
      - name: app-network
  vars:
    ansible_python_interpreter: /opt/docker_venv/bin/python