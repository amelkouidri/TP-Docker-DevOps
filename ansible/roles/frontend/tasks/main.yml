---
- name: Run frontend
  docker_container:
    name: frontend
    image: amelkdr/tp-devops-frontend:latest
    ports:
      - "8080:80"
    networks:
      - name: app-network
  vars:
    ansible_python_interpreter: /opt/docker_venv/bin/python